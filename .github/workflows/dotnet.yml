name: dotnet

on:
  pull_request:

permissions: {}

jobs:
  compute:
    permissions:
      contents: read
    runs-on: ubuntu-latest
    steps:
    - name: Clone repository
      uses: actions/checkout@v4
    - name: Clone dotnet repo
      run: |
        git clone https://github.com/dotnet/dotnet --depth 1
    - name: Prep
      run: |
        cd dotnet
        pwd
        ./prep-source-build.sh
        git status
    - name: Build
      run: |
        ./build.sh
        git status
